diff --git a/src/lib/components/chat/Chat.svelte b/src/lib/components/chat/Chat.svelte
index 45c2e01e2..a39e384c7 100644
--- a/src/lib/components/chat/Chat.svelte
+++ b/src/lib/components/chat/Chat.svelte
@@ -134,6 +134,7 @@
 	let eventCallback = null;
 
 	let selectedModels = [''];
+	let selectedKBIds: number[] = [];
 	let atSelectedModel: Model | undefined;
 	let selectedModelIds = [];
 	$: if (atSelectedModel !== undefined) {
@@ -2758,6 +2759,7 @@
 						{history}
 						title={$chatTitle}
 						bind:selectedModels
+						bind:selectedKBIds
 						shareEnabled={!!history.currentId}
 						{initNewChat}
 						{archiveChatHandler}
diff --git a/src/lib/components/chat/KBSelector.svelte b/src/lib/components/chat/KBSelector.svelte
index 3c4c87ff2..f205c18dc 100644
--- a/src/lib/components/chat/KBSelector.svelte
+++ b/src/lib/components/chat/KBSelector.svelte
@@ -1,26 +1,28 @@
 <script lang="ts">
-	import { getAvailableKBs, setChatKBConfig } from '$lib/apis/kb';
 	import { onMount } from 'svelte';
+	import { getAvailableKBs, setChatKBConfig } from '$lib/apis/kb';
 
 	export let token: string = '';
 	export let chatId: string = '';
+	export let selectedKBIds: number[] = [];
 
 	let kbs: any[] = [];
-	let selectedKBIds: number[] = [];
 	let open = false;
 	let loading = true;
 
 	onMount(async () => {
-		if (token) {
-			try {
-				kbs = await getAvailableKBs(token);
-			} catch (e) {
-				console.warn('Failed to load KBs:', e);
-				kbs = [];
-			}
+		await loadKBs();
+	});
+
+	async function loadKBs() {
+		if (!token) return;
+		try {
+			kbs = await getAvailableKBs(token);
+		} catch (e) {
+			kbs = [];
 		}
 		loading = false;
-	});
+	}
 
 	async function toggleKB(kbId: number) {
 		if (selectedKBIds.includes(kbId)) {
@@ -30,18 +32,31 @@
 		}
 		if (chatId) {
 			const config = selectedKBIds.map((id) => ({ kb_id: id, subtag_ids: [] }));
-			await setChatKBConfig(token, chatId, config);
+			try {
+				await setChatKBConfig(token, chatId, config);
+			} catch (e) {
+				console.warn('Failed to save KB config:', e);
+			}
 		}
 	}
+
+	function handleClickOutside() {
+		if (open) open = false;
+	}
 </script>
 
+<svelte:window on:click={handleClickOutside} />
+
 {#if kbs.length > 0}
-	<div class="relative inline-block">
+	<!-- svelte-ignore a11y-click-events-have-key-events -->
+	<!-- svelte-ignore a11y-no-static-element-interactions -->
+	<div class="relative inline-block" on:click|stopPropagation>
 		<button
 			class="flex items-center gap-1.5 px-2.5 py-1 text-xs font-medium rounded-lg
-				   bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700
-				   text-gray-700 dark:text-gray-300 transition-colors"
+				   bg-gray-50 dark:bg-gray-850 hover:bg-gray-100 dark:hover:bg-gray-800
+				   text-gray-600 dark:text-gray-300 transition-colors border border-gray-200 dark:border-gray-700"
 			on:click={() => (open = !open)}
+			title="Select Knowledge Bases for this chat"
 		>
 			<svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none"
 				stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
@@ -49,34 +64,40 @@
 				<path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z" />
 			</svg>
 			{#if selectedKBIds.length === 0}
-				Knowledge Bases
+				KBs
 			{:else}
-				{selectedKBIds.length} KB{selectedKBIds.length > 1 ? 's' : ''} selected
+				{selectedKBIds.length} KB{selectedKBIds.length > 1 ? 's' : ''}
 			{/if}
-			<svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3 transition-transform {open ? 'rotate-180' : ''}"
+			<svg xmlns="http://www.w3.org/2000/svg"
+				class="w-3 h-3 transition-transform duration-200 {open ? 'rotate-180' : ''}"
 				viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
 				<polyline points="6 9 12 15 18 9" />
 			</svg>
 		</button>
 
 		{#if open}
-			<div class="absolute left-0 mt-1 w-64 bg-white dark:bg-gray-900 rounded-lg shadow-lg
-						border border-gray-200 dark:border-gray-700 z-50 max-h-60 overflow-y-auto">
+			<div class="absolute left-0 mt-1.5 w-72 bg-white dark:bg-gray-900 rounded-xl shadow-xl
+						border border-gray-200 dark:border-gray-700 z-[999] max-h-72 overflow-y-auto">
+				<div class="px-3 py-2 border-b border-gray-100 dark:border-gray-800">
+					<span class="text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide">
+						Knowledge Bases
+					</span>
+				</div>
 				{#if loading}
-					<div class="p-3 text-sm text-gray-500">Loading...</div>
+					<div class="p-4 text-sm text-gray-400 text-center">Loading...</div>
 				{:else if kbs.length === 0}
-					<div class="p-3 text-sm text-gray-500">No knowledge bases available</div>
+					<div class="p-4 text-sm text-gray-400 text-center">No knowledge bases available</div>
 				{:else}
-					{#each kbs as kb}
+					{#each kbs as kb (kb.id)}
 						<button
-							class="w-full flex items-center gap-2 px-3 py-2 text-left text-sm
-								   hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors"
+							class="w-full flex items-center gap-3 px-3 py-2.5 text-left text-sm
+								   hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors"
 							on:click={() => toggleKB(kb.id)}
 						>
-							<div class="w-4 h-4 rounded border-2 flex items-center justify-center
+							<div class="w-5 h-5 rounded flex-shrink-0 border-2 flex items-center justify-center transition-colors
 									{selectedKBIds.includes(kb.id)
 										? 'bg-blue-500 border-blue-500'
-										: 'border-gray-400 dark:border-gray-600'}">
+										: 'border-gray-300 dark:border-gray-600'}">
 								{#if selectedKBIds.includes(kb.id)}
 									<svg class="w-3 h-3 text-white" fill="none" viewBox="0 0 24 24"
 										stroke="currentColor" stroke-width="3">
@@ -85,9 +106,9 @@
 								{/if}
 							</div>
 							<div class="flex-1 min-w-0">
-								<div class="font-medium truncate">{kb.name}</div>
+								<div class="font-medium text-gray-800 dark:text-gray-200 truncate">{kb.name}</div>
 								{#if kb.description}
-									<div class="text-xs text-gray-500 dark:text-gray-400 truncate">
+									<div class="text-xs text-gray-400 dark:text-gray-500 truncate mt-0.5">
 										{kb.description}
 									</div>
 								{/if}
diff --git a/src/lib/components/chat/Navbar.svelte b/src/lib/components/chat/Navbar.svelte
index 5c4e1364b..ed9d1443f 100644
--- a/src/lib/components/chat/Navbar.svelte
+++ b/src/lib/components/chat/Navbar.svelte
@@ -39,6 +39,7 @@
 	import ChatCheck from '../icons/ChatCheck.svelte';
 	import Knobs from '../icons/Knobs.svelte';
 	import { WEBUI_API_BASE_URL } from '$lib/constants';
+	import KBSelector from './KBSelector.svelte';
 
 	const i18n = getContext('i18n');
 
@@ -50,6 +51,7 @@
 	export let history;
 	export let selectedModels;
 	export let showModelSelector = true;
+	export let selectedKBIds: number[] = [];
 
 	export let onSaveTempChat: () => {};
 	export let archiveChatHandler: (id: string) => void;
@@ -111,9 +113,17 @@
 			{$showSidebar ? 'ml-1' : ''}
 			"
 				>
-					{#if showModelSelector}
+					{#if showModelSelector && $user?.role === 'admin'}
 						<ModelSelector bind:selectedModels showSetDefault={!shareEnabled} />
 					{/if}
+
+					<div class="mt-1">
+						<KBSelector
+							token={localStorage.token}
+							chatId={$chatId}
+							bind:selectedKBIds
+						/>
+					</div>
 				</div>
 
 				<div class="self-start flex flex-none items-center text-gray-600 dark:text-gray-400">
